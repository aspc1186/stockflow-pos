import { useRef, useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { CalendarDays, ImageUp, Plus, Trash2 } from 'lucide-react'
import api from '@/lib/axios'
import Modal from '@/components/ui/Modal'
import { formatDate } from '@/lib/utils'
import toast from 'react-hot-toast'

export default function ReservasEventosPage({modo}:{modo:'reservas'|'eventos'}) {
  const qc=useQueryClient(), esReserva=modo==='reservas', input=useRef<HTMLInputElement>(null)
  const [open,setOpen]=useState(false),[form,setForm]=useState<any>(esReserva?{nombre:'',telefono:'',fecha_hora:'',personas:'2',notas:'',estado:'pendiente'}:{titulo:'',descripcion:'',fecha_inicio:'',fecha_fin:'',tipo:'evento',imagen_url:'',activo:true})
  const {data:registros=[]}=useQuery({queryKey:[modo],queryFn:async()=>{const {data}=await api.get(`/${modo}`);return data.data||data}})
  const guardar=useMutation({mutationFn:()=>api.post(`/${modo}`,{...form,personas:Number(form.personas)||1}),onSuccess:()=>{qc.invalidateQueries({queryKey:[modo]});setOpen(false);toast.success(esReserva?'Reserva creada':'Evento publicado')},onError:(e:any)=>toast.error(e?.response?.data?.msg||'No se pudo guardar')})
  const eliminar=useMutation({mutationFn:(id:string)=>api.delete(`/${modo}/${id}`),onSuccess:()=>qc.invalidateQueries({queryKey:[modo]})})
  const cargar=(file?:File)=>{if(!file)return;const r=new FileReader();r.onload=()=>setForm((p:any)=>({...p,imagen_url:String(r.result||'')}));r.readAsDataURL(file)}
  return <div className="space-y-5"><div className="page-header"><div><h1 className="page-title">{esReserva?'Reservas':'Eventos y promociones'}</h1><p className="page-subtitle">{registros.length} registros</p></div><button className="btn-primary btn-sm" onClick={()=>setOpen(true)}><Plus className="h-4 w-4"/>{esReserva?'Nueva reserva':'Nuevo evento'}</button></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{registros.map((r:any)=><article key={r.id} className="card overflow-hidden">{!esReserva&&r.imagen_url&&<img src={r.imagen_url} className="h-32 w-full object-cover"/>}<div className="p-4"><div className="flex justify-between gap-3"><div><h2 className="font-semibold">{esReserva?r.nombre:r.titulo}</h2><p className="mt-1 text-xs text-surface-200/50">{formatDate(esReserva?r.fecha_hora:r.fecha_inicio,'dd/MM/yyyy HH:mm')}</p></div><button className="btn-ghost btn-sm p-2 text-red-300" onClick={()=>eliminar.mutate(r.id)}><Trash2 className="h-4 w-4"/></button></div><p className="mt-3 text-sm text-surface-200/65">{esReserva?`${r.personas} personas · ${r.telefono||'Sin teléfono'}`:r.descripcion||'Sin descripción'}</p><span className="badge-gray mt-3">{esReserva?r.estado:r.tipo}</span></div></article>)}</div>{!registros.length&&<div className="card py-16 text-center text-surface-200/45">Aún no hay {esReserva?'reservas':'eventos o promociones'}.</div>}<Modal open={open} onClose={()=>setOpen(false)} title={esReserva?'Nueva reserva':'Nuevo evento o promoción'} footer={<div className="flex gap-3"><button className="btn-secondary flex-1" onClick={()=>setOpen(false)}>Cancelar</button><button className="btn-primary flex-1" onClick={()=>guardar.mutate()} disabled={guardar.isPending}>{guardar.isPending?'Guardando...':'Guardar'}</button></div>}>{esReserva?<div className="space-y-3"><input className="input" placeholder="Nombre" value={form.nombre} onChange={e=>setForm({...form,nombre:e.target.value})}/><input className="input" placeholder="Teléfono" value={form.telefono} onChange={e=>setForm({...form,telefono:e.target.value})}/><div className="grid grid-cols-2 gap-3"><input type="datetime-local" className="input" value={form.fecha_hora} onChange={e=>setForm({...form,fecha_hora:e.target.value})}/><input type="number" className="input" placeholder="Personas" value={form.personas} onChange={e=>setForm({...form,personas:e.target.value})}/></div><textarea className="input" placeholder="Notas" value={form.notas} onChange={e=>setForm({...form,notas:e.target.value})}/></div>:<div className="space-y-3"><input className="input" placeholder="Título" value={form.titulo} onChange={e=>setForm({...form,titulo:e.target.value})}/><textarea className="input" placeholder="Descripción de la promoción" value={form.descripcion} onChange={e=>setForm({...form,descripcion:e.target.value})}/><div className="grid grid-cols-2 gap-3"><input type="datetime-local" className="input" value={form.fecha_inicio} onChange={e=>setForm({...form,fecha_inicio:e.target.value})}/><input type="datetime-local" className="input" value={form.fecha_fin} onChange={e=>setForm({...form,fecha_fin:e.target.value})}/></div><input ref={input} type="file" accept="image/*" className="hidden" onChange={e=>cargar(e.target.files?.[0])}/><button className="btn-secondary btn-sm" onClick={()=>input.current?.click()}><ImageUp className="h-4 w-4"/>Subir imagen</button>{form.imagen_url&&<img src={form.imagen_url} className="h-24 w-40 rounded-lg object-cover"/>}</div>}</Modal></div>
}
